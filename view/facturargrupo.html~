{include="header"}

{if="$fsc->documento"}
<form name="f_copiar_documento" action="{$fsc->url()}" method="POST" class="form">

   <input type="hidden" name="generar" value="TRUE">

   <div class="container-fluid">
       <div class="row">
         <div class="col-sm-4">
            <div class="page-header">
               <h1>
                  <a href="{$fsc->documento->url()}" class="btn btn-xs btn-default">
                     <span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span>
                  </a>
                  Facturar a grupo una copia de <small>{$fsc->documento->codigo}</small>
               </h1>
            </div>
         </div>
      </div>  


  <div class="row">
      <div class="col-sm-12">
         <h4>
            Haz click en el grupo de clientes al que quieres generar la misma factura.
            <mark>Recuerda que si el cliente original está en el grupo no lo incluirá en esta nueva generación de facturas.</mark>
         </h4>
         <div class="table-responsive">
            <table class="table table-hover">
               <thead>
                  <tr>
                     <th>CodGrupo</th>
                     <th>Nombre</th>
                  </tr>
               </thead>
               {loop="$fsc->gclientes"}
               <tr class="clickableRow" href="{$fsc->url()}&codgrupo={$value['codgrupo']}">
                  <td>{$value['codgrupo']}</td>
                  <td>{$value['nombre']}</td>
               </tr>
               {/loop}
            </table>
         </div>
      </div>
   </div>










   {if="$fsc->clientes"}

  <div class="row">
      <div class="col-sm-12">
         <h4>
            Estos son los clientes sobre los que se procederá a generar facturas.
         </h4>
         <div class="table-responsive">
            <table class="table table-hover">
               <thead>
                  <tr>
                     <th>CodCliente</th>
                     <th>Nombre</th>
                  </tr>
               </thead>
               {loop="$fsc->clientes"}
               <tr>
                  <td>{$value['codcliente']}</td>
                  <td>{$value['nombre']}</td>
               </tr>
               {/loop}
            </table>
         </div>
      </div>
   </div>
   
   
      <div class="row">
         <div class="col-sm-12 text-right">
            <!--<a href="{$fsc->url()}&generar=TRUE" class="btn btn-sm btn-primary">
             <span class="glyphicon glyphicon-arrow-right"></span>
               Generar
            </a>-->
            
            <button class="btn btn-sm btn-primary" type="submit">
               <span class="glyphicon glyphicon-arrow-right"></span>
               <span class="hidden-xs">&nbsp; Generar</span>
            </button>            
            
            
         </div>
      </div>   
   
   

   {/if}



   
   
   
   </div>

</form>
{else}
<h1>No se ha encontrado nada.</h1>
{/if}

{include="footer"}